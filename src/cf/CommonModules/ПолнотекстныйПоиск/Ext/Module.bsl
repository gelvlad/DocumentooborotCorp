
&НаСервере
Процедура ИзвлечьСодержаниеФайла() Экспорт
	Выборка = Справочники.Файлы.Выбрать(,,Новый Структура("Изменен", Истина));
	
	Пока Выборка.Следующий() Цикл
		Файл = Выборка.ПолучитьОбъект();
		Файл.Изменен = Ложь;
		
		Если Не Файл.Расширение = ".txt" Тогда
			Файл.Записать();
			Продолжить;
		КонецЕсли;
		
		ЧтениеДанных = Новый ЧтениеДанных(Файл.Файл.Получить());
		Файл.Содержание = ЧтениеДанных.ПрочитатьСимволы();
		Файл.Записать();
	КонецЦикла;
КонецПроцедуры
